import React, {Componen}from 'react';
import {Text} from 'react-native';

export default class DetailStack extends Componen {
    render(){
        return(
            <Text>DetailStack</Text>
        )
    }
}